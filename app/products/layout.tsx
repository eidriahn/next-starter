import { type FC, type PropsWithChildren } from "react";

export const ProductsLayout: FC<PropsWithChildren> = ({ children }) => {
  return <div>{children}</div>;
};

export default ProductsLayout;
